// Code your design here
module counter (
  input clk,
  input rstn,
  input en,
  output reg [3:0] q
);
  
  always @(posedge clk)begin
    if (!rstn)
      q<=0;
    else if(en) begin
      if(q==9)
        q<=0;
      else
        q<=q+1;
    end
  end
  endmodule